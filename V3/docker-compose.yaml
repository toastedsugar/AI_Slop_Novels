services:
  server:
    build: ./server
    working_dir: /app
    volumes:
      - ./server/app:/app/app
    env_file:
      - ./.env
    ports:
      - "8000:8000"

  client:
    build: ./client
    working_dir: /app
    volumes:
      - ./client:/app
      - /app/node_modules
    ports:
      - "5173:5173"
