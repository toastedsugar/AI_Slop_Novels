services:
  novelgen:
    build: .
    working_dir: /app
    volumes:
      - .:/app
      - ./outputs:/app/outputs
    environment:
      - ENV=nsfw-test
    env_file:
      - .env
    command: python src/main.py
