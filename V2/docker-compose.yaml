services:
  novelgen:
    build: .
    working_dir: /app
    volumes:
      - .:/app
      - ./outputs:/app/outputs
    environment:
      - ENV=or-dev
    env_file:
      - .env
    networks:
      - novel-net
    command: python src/main.py

networks:
  novel-net:
    driver: bridge
