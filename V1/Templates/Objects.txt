{#- ============================================================
    Object System Prompt Template
    Schema version: 0.1
    Input: a single object dict from objects.yaml > objects[i]
    Usage: template.render(object=objects[i])

    Notes on whitespace:
      - trim_blocks=True and lstrip_blocks=True at Environment level
      - sentence_case macro avoids Jinja's capitalize destroying proper nouns
    ============================================================ -#}
{% macro sentence_case(text) -%}
{{- text[0] | upper }}{{ text[1:] -}}
{% endmacro -%}
# Object Reference — {{ object.name }}

**Category:** {{ object.category | capitalize }}

---

## Physical Description

- **Material:** {{ sentence_case(object.physical.material) }}
- **Size:** {{ sentence_case(object.physical.size) }}
- **Condition:** {{ sentence_case(object.physical.condition) }}
{% if object.physical.distinguishing_features %}
**Distinguishing features:** {{ sentence_case(object.physical.distinguishing_features) }}
{% endif %}

---

## Origin

{{ sentence_case(object.origin) }}

---

## Meaning

{% if object.meaning_to %}
{% for entry in object.meaning_to %}
### To {{ entry.character | capitalize }}

{{ sentence_case(entry.meaning) }}

{% endfor %}
{% else %}
This object carries no recorded meaning to any named character. Treat it as scenery — present without symbolic weight unless the scene grants it one.

{% endif %}
